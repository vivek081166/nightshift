startswith('P')      46 / 50
re.search('P[123]')  50 / 50
  dropped: **P1** - Roll back the deployment from 14 minutes ago.
  dropped: **P1** — Roll back the deployment from 14 minutes ago.
  dropped: **P1** — Roll back the deployment from 14 minutes ago.
  dropped: **P1** — Roll back the deployment from 14 minutes ago immediately.
